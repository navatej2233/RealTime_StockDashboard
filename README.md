# Real-Time Stock Dashboard

A Streamlit dashboard to visualize live stock data using Yahoo Finance.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py

then do:

```bash
git add README.md
git commit -m "added README"
git push
